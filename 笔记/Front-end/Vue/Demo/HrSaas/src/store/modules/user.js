// 储存 用户相关的数据

const state = {}
const mutations = {}
const actions = {}
const getters = {}

export default {
  // 开启命名空间  => 先找模块, 再找方法
  namespaced: true,
  state,
  mutations,
  actions,
  getters
}
